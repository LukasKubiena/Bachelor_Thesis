# input data

data files are not included in the submission because some corpora have restricted licences.

run `python src/01_load_data.py --lang de` or `--lang en` to download and clean the open corpora. for the gated german corpora, follow the instructions in the main `README.md` first.
